import React from "react";
import RecipeCard from "../ui/RecipeCard";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <RecipeCard />
    </div>
  );
};

export default page;
