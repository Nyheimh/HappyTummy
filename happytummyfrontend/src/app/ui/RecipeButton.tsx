import { Button } from "@chakra-ui/react";

export default function RecipeButton() {
  return <Button colorScheme="blue">Button</Button>;
}

// Incorporate a disabled link for button when filter is active
