import React from 'react';
declare const HeroButton: ({ colorScheme, size, text }: {
    colorScheme?: string;
    size?: string;
    text?: string;
}) => React.JSX.Element;
export default HeroButton;
